
const browserVersion='chrome'; //global

function getBrowserVersion()
{
    if(browserVersion.startsWith('chrome'))
        
        {
            var version='chrome 12.1';
        }
    console.log(version);
    
}
//console.log(version);
console.log(browserVersion);
getBrowserVersion();